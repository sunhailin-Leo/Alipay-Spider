{
    "db_name":"./../db/alipay.db",
    "db_init_sql":"CREATE TABLE IF NOT EXISTS t_alipay_bill_info (time varchar(50) primary key NOT NULL, memo varchar(255), name varchar(100), seller_code varchar(100), transfer_code varchar(100), serial_num varchar(100), opposite varchar(100), money varchar(100), status varchar(100))",
    "db_insert_sql":"INSERT INTO t_alipay_bill_info VALUES (?,?,?,?,?,?,?,?,?)"
}