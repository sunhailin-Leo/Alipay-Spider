{
    "db_name":"./../db/alipay.db",
    "db_init_sql":"CREATE TABLE IF NOT EXISTS t_alipay_transfer_info (transfer_id varchar(50) primary key NOT NULL, transfer_ymd varchar(8),transfer_hms varchar(6),transfer_memo varchar(255), transfer_name varchar(100), transfer_seller_code varchar(100), transfer_transfer_code varchar(100), transfer_serial_num varchar(100), transfer_opposite varchar(100), transfer_money varchar(100), transfer_status varchar(100),transfer_username varchar(100)transfer_tag varchar(100))",
    "db_insert_sql":"INSERT INTO t_alipay_transfer_info VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)"
}